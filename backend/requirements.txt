# OCR — used by capture/ocr.py and analysis/game_phase.py (legacy / dev path only)
# The main data pipeline uses the Riot Live Client Data API instead.
winocr==0.0.15
Pillow==10.4.0

# AI
anthropic==0.40.0

# API server
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Data models
pydantic==2.10.3
pydantic-settings==2.7.0

# HTTP client (scraper + Riot live client API probe)
httpx==0.28.1

# Riot Games API wrapper (Match-V5, rate limiting)
riotwatcher==3.3.1

# Windows process detection (used by lol_phase.py)
psutil==7.2.2

# Environment
python-dotenv==1.0.1

# Dev / test
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
ruff==0.8.4
